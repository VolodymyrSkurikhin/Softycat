import { AboutContainer, AboutText, aboutContent } from "../components/about";

export const About = () => {
  return (<AboutContainer>
    <AboutText>{aboutContent}</AboutText>
  </AboutContainer>)
}